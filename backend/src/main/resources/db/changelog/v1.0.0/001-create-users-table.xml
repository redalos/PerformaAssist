<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <changeSet id="001-create-users-table" author="performaassist">
        <comment>Création de la table des utilisateurs</comment>
        
        <createTable tableName="users">
            <column name="id" type="UUID">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="email" type="VARCHAR(255)">
                <constraints nullable="false" unique="true"/>
            </column>
            <column name="password_hash" type="VARCHAR(255)">
                <constraints nullable="false"/>
            </column>
            <column name="first_name" type="VARCHAR(100)">
                <constraints nullable="false"/>
            </column>
            <column name="last_name" type="VARCHAR(100)">
                <constraints nullable="false"/>
            </column>
            <column name="role" type="VARCHAR(20)">
                <constraints nullable="false"/>
            </column>
            <column name="site_id" type="UUID">
                <constraints nullable="true"/>
            </column>
            <column name="is_active" type="BOOLEAN" defaultValueBoolean="true">
                <constraints nullable="false"/>
            </column>
            <column name="last_login_at" type="TIMESTAMP">
                <constraints nullable="true"/>
            </column>
            <column name="created_at" type="TIMESTAMP" defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>
            <column name="updated_at" type="TIMESTAMP" defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>
            <column name="created_by" type="UUID">
                <constraints nullable="true"/>
            </column>
            <column name="updated_by" type="UUID">
                <constraints nullable="true"/>
            </column>
        </createTable>

        <!-- Index sur l'email pour les recherches rapides -->
        <createIndex tableName="users" indexName="idx_users_email">
            <column name="email"/>
        </createIndex>

        <!-- Index sur le rôle -->
        <createIndex tableName="users" indexName="idx_users_role">
            <column name="role"/>
        </createIndex>

        <!-- Index sur le site -->
        <createIndex tableName="users" indexName="idx_users_site_id">
            <column name="site_id"/>
        </createIndex>

        <!-- Index sur is_active pour filtrer les utilisateurs actifs -->
        <createIndex tableName="users" indexName="idx_users_is_active">
            <column name="is_active"/>
        </createIndex>

        <!-- Contrainte de vérification pour le rôle -->
        <sql>
            ALTER TABLE users ADD CONSTRAINT chk_users_role 
            CHECK (role IN ('ADMIN', 'MANAGER', 'EMPLOYEE'));
        </sql>

    </changeSet>

</databaseChangeLog>

